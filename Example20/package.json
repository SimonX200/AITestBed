{
  "name": "session-manager",
  "version": "1.0.0",
  "description": "Session Manager with persistent storage and HTTP API",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "esbuild src/sessionManager.ts --bundle --outfile=dist/bundle.js --platform=node --target=node20",
    "test": "jest --forceExit --detectOpenHandles --testPathIgnorePatterns=e2e",
    "e2e": "npm test && bash deploy.sh e2e",
    "e2e:setup": "bash deploy.sh e2e-setup",
    "e2e:teardown": "bash deploy.sh e2e-teardown"
  },
  "dependencies": {
    "@types/sql.js": "^1.4.11",
    "express": "^4.21.0",
    "sql.js": "^1.14.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "@types/supertest": "^7.2.1",
    "esbuild": "^0.20.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "ts"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ],
    "roots": [
      "<rootDir>"
    ]
  },
  "allowScripts": {
    "better-sqlite3@11.10.0": true,
    "esbuild@0.20.2": true
  }
}
