{
    "name": "session-manager",
    "version": "1.0.0",
    "description": "Session Manager with Redis persistence and HTTP API",
    "main": "dist/bundle.js",
    "scripts": {
        "build": "esbuild src/sessionManager.ts --bundle --outfile=dist/bundle.js --platform=node --target=node20",
        "test": "jest",
        "e2e": "jest --config jest.e2e.config.js"
    },
    "dependencies": {
        "express": "^4.18.2",
        "redis": "^4.6.7"
    },
    "devDependencies": {
        "@types/express": "^4.17.17",
        "@types/jest": "^30.0.0",
        "@types/node": "^20.4.5",
        "@types/supertest": "^2.0.12",
        "esbuild": "^0.19.5",
        "jest": "^29.6.4",
        "supertest": "^6.3.3",
        "ts-jest": "^29.1.1",
        "typescript": "^5.1.6"
    }
}
