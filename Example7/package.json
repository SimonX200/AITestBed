{
  "name": "example7-session-manager",
  "version": "1.0.0",
  "description": "Session Manager with HTTP API",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "npx esbuild sessionManager.ts --bundle --outfile=dist/bundle.js --platform=node --minify",
    "test": "npx ts-mocha tests/sessionManager.test.ts --paths",
    "e2e": "bash e2e-run.sh"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "ts-mocha": "^10.0.0",
    "mocha": "^10.2.0",
    "chai": "^4.3.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  },
  "allowScripts": {
    "esbuild@0.20.2": true
  }
}
