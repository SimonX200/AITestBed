{
  "name": "example6-session-manager",
  "version": "1.0.0",
  "description": "Session Manager with Docker deployment",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "npx esbuild server.ts --bundle --outfile=dist/bundle.js --platform=node --target=node20",
    "test": "npx tsx sessionManager.test.ts",
    "e2e": "bash run-e2e.sh"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
